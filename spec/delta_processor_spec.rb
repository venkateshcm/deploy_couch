require File.dirname(__FILE__) + '/spec_helper'

describe DeltaProcessor, "execute a delta" do
  it "load relavent documents to apply delta" do
    #delta = DeltaProcessor.new("customer","function map(doc){ doc.name = 'test';}",Repository.new)
  end
end